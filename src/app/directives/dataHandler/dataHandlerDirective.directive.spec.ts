/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DataHandlerDirective } from './dataHandlerDirective.directive';

describe('Directive: DataHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new DataHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
